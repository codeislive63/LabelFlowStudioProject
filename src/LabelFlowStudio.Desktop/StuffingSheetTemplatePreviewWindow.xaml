<Window x:Class="LabelFlowStudio.Desktop.StuffingSheetTemplatePreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Лист сброса"
        Height="750"
        Width="900">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="12">

            <Button x:Name="PrintButton"
                    Content="Печать"
                    Click="OnPrintClick"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    IsEnabled="False"/>

            <TextBlock x:Name="StatusText"
                       VerticalAlignment="Center"
                       Margin="0,0,8,0"/>

            <Button Content="Закрыть" Click="OnCloseClick" Padding="12,6"/>
        </StackPanel>

        <Border Margin="12" BorderBrush="Gray" BorderThickness="1">
            <wv2:WebView2 x:Name="PreviewWebView"
                         NavigationCompleted="OnNavigationCompleted" />
        </Border>
    </DockPanel>
</Window>
