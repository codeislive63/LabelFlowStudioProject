<UserControl x:Class="LabelFlowStudio.Desktop.Views.Work.WorkSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:LabelFlowStudio.Desktop.ViewModels"
             xmlns:workViews="clr-namespace:LabelFlowStudio.Desktop.Views.Work"
             d:DataContext="{d:DesignInstance Type=viewModels:WorkSectionViewModel}"
             d:DesignHeight="760"
             d:DesignWidth="1200"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type viewModels:AutomaticLineViewModel}">
            <workViews:AutomaticLineView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:ManualProcessingViewModel}">
            <workViews:ManualProcessingView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="{DynamicResource ShellPagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <workViews:WorkModeSelector
            Grid.Row="0"
            Margin="0,0,0,12"
            HorizontalAlignment="Left"
            DataContext="{Binding CurrentModeViewModel.Work}" />

        <ContentControl
            Grid.Row="1"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            Content="{Binding CurrentModeViewModel}" />
    </Grid>
</UserControl>
